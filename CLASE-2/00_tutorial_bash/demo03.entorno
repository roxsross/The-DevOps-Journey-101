#!/bin/bash
#Ejemplo 3: accedemos a variables de entorno
echo Ejemplos de variables de entorno:
echo ---------------------------------
echo Versión de BASH..: $BASH_VERSION
echo Directorio HOME..: $HOME
echo Nombre del equipo: $HOSTNAME
echo Usuario..........: $USERNAME
exit 0
